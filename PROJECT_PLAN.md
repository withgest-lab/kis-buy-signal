# 프로젝트 개요 — KIS 매수신호 알림 시스템

> **Claude Code 사용 안내**: 이 파일 전체를 먼저 읽고 프로젝트 목적/맥락/제약사항을
> 전부 파악한 뒤, 맨 아래 "실행 계획"의 STEP 0부터 사용자와 함께 순서대로
> 진행해줘. 각 STEP은 "작업"과 "검증"으로 구성되어 있고, 검증 결과를
> 사용자에게 보여준 뒤 확인받고 나서만 다음 STEP으로 넘어가줘. 사용자가
> 명시적으로 "다음 단계 진행해줘" 또는 이에 준하는 확인을 하기 전까지는
> 앞서가지 마.

---

## 1. 프로젝트 목적

한국투자증권(KIS) Open API를 통해 미국주식 시세를 조회하고, 기술적 지표
기반 스코어링으로 매수 후보 종목을 판별해서 텔레그램으로 알림을 보내는
시스템을 만든다.

**중요한 범위 제한 (Phase 1 vs Phase 2)**
- **지금(Phase 1)은 조회 + 알림만 한다. 실제 매수/매도 주문 기능은 절대
  구현하지 않는다.** 사용자는 알림을 받으면 별도의 MTS 앱으로 직접 수동
  매매한다.
- 실제 자동매매(Phase 2)는 Phase 1 결과를 몇 주간 관찰해서 신호 품질을
  검증한 뒤, 별도로 다시 설계해서 진행할 예정. 지금 단계에서 주문 관련
  API 함수는 코드에 포함하지 않는다.

---

## 2. 타겟 종목 리스트 (32개)

```
빅테크 10: NVDA, AAPL, GOOGL, MSFT, AMZN, META, AVGO, TSLA, ORCL, PLTR
산업대장 12: CRWD, PANW, TSM, ISRG, ROK, IONQ, BE, GEV, VRT, COHR, RKLB, LMT
섹터ETF 10: SPY, QQQ, CIBR, BUG, SOXX, BOTZ, ROBO, AIPO, QTUM, VOLT
```

각 종목당 최소 일봉 250봉(1년), 주봉 104봉(2년) 데이터 필요
(ADX/이평선/다이버전스 계산 워밍업 구간 확보를 위해).

---

## 3. 매수신호 스코어링 로직 (핵심 설계)

단순 RSI<30 같은 임계값 교차 대신, 다중 요소를 가중 합산한 0~100점
스코어로 판정한다. 목적은 "바닥 정확히 잡기"가 아니라 "확률 높은 진입
구간 포착".

### 3-1. 기본 지표
- **RSI(14)**: Wilder's smoothing (`ewm(alpha=1/14, adjust=False)`)
- **MFI(14)**: Typical Price(고저종평균)×거래량 기반 14일 자금흐름비율
- **볼린저밴드(20, 2σ)**: 중심선(20일 SMA) ± 2×표준편차
  - `%B = (종가-하단)/(상단-하단)` — 0에 가까울수록 하단 근접
  - `Bandwidth = (상단-하단)/중심선` — 변동성 수축/확장 측정
- **ADX(14)**: +DI, -DI, DX 표준공식. 추세 강도 판정용
- **20주 이동평균**: 주봉 종가 기준, 대세 추세 필터용

### 3-2. 파생 신호
1. **RSI/MFI 강세 다이버전스**: 최근 30일 내 가격 저점 2개 비교 — 가격은
   신저점인데 지표(RSI/MFI) 저점은 더 높으면 감지 (반전 신뢰도 높은 신호)
2. **볼린저 스퀴즈→확장**: Bandwidth가 최근 120일 하위 20% 구간(스퀴즈)에
   있다가 반등(전일 대비 확장 전환)하면 감지 (변동성 확장 임박)
3. **MFI 선행반등**: 최근 5일 내 MFI가 RSI보다 먼저 반등 전환 (거래량이
   모멘텀보다 먼저 바닥 형성 = 신뢰도 높은 선행지표)
4. **거래량 소진**: 최근 5일 평균 거래량 < 최근 20일 평균 거래량 (매도
   압력 소진 신호)

### 3-3. 스코어링 공식
```
최종점수 = 주봉추세필터×25 + %B하단근접×20 + RSI다이버전스×20
         + MFI선행반등×15 + 볼린저스퀴즈확장×10 + 거래량소진×10
```

**각 항목 0~1 정규화 규칙**
```
주봉추세필터:
  종가>20주선 AND 기울기>0        → 1.0
  종가>20주선                     → 0.6
  20주선 대비 -3% 이내             → 0.3
  그 외                           → 0.0

%B하단근접:
  %B <= 0.05                     → 1.0
  %B <= 0.2                      → (0.2-%B)/0.15  (선형보간)
  그 외                           → 0.0

RSI다이버전스:
  감지됨                          → 0.7 + 강도점수×0.3
  아니면                          → 0.0

MFI선행반등:      감지시 1.0 / 아니면 0.0
볼린저스퀴즈확장:  감지시 1.0 / 아니면 0.0

거래량소진:
  최근5일평균 < 하락구간평균×0.8   → 1.0
  최근5일평균 < 하락구간평균       → 0.5
  그 외                           → 0.0
```

**추세 레짐 페널티 (마지막에 적용)**
```
ADX >= 25 AND -DI > +DI (강한 하락추세 진행 중):
    최종점수 = 최종점수 × 0.3
```
→ 하락추세 중 과매도 신호만 보고 진입하는 걸 방지하기 위한 감점 장치.
완전 배제가 아니라 대폭 감점만 함.

**판정 구간**
- 70점 이상: 강한 매수 후보
- 50~70점: 관찰 대상(워치리스트)
- 50점 미만: 무시

### 3-4. 필수 검증 원칙 (구현 후 반드시 적용)
- 요소를 하나씩 껐다 켰다 하며 백테스트로 실제 기여도 확인 (기여 없는
  요소는 가중치 낮추거나 제거 — 과최적화 방지)
- 여러 종목·여러 기간에 걸쳐 같은 가중치로 검증 (특정 종목에만 잘 맞으면
  과적합 의심)
- 인샘플(과거 튜닝 구간)과 아웃오브샘플(이후 구간)을 나눠서 검증

---

## 4. 알림 방식

- **채널**: 텔레그램 (봇 토큰 + Chat ID 이미 발급 완료)
- **조건**: "어제는 70점 미만이었는데 오늘 70점 이상이 된 종목"만 알림
  (상태 변화 감지 — 매일 같은 종목이 계속 70점이어도 반복 알림 안 감)
- **알림 내용**: 종목명, 최종점수, 감지된 세부신호(다이버전스/스퀴즈 등),
  현재가
- **실행 주기**: 하루 1회, 미국장 마감 후 기준 데이터로 계산 (실시간
  대응 불필요 — 일봉/주봉 기반이므로)
- **실행 시각**: 한국시간 오전 7:30 (사용자 기상 시간대에 맞춤, 조정 가능)

---

## 5. 실행 인프라

**요구사항**: 데스크탑/노트북을 특정 시간에 상시 켜둘 필요 없이, 모바일로
알림만 편하게 받고 싶음.

**해결책**: GitHub Actions
- 코드는 로컬(Windows 데스크탑/노트북)에서 작성
- 완성된 코드는 **GitHub Private 저장소**에 push
- GitHub Actions가 매일 정해진 시각에 GitHub 서버에서 자동 실행
  (사용자 기기는 켜져 있을 필요 없음)
- API 키/토큰은 GitHub Secrets에 암호화 등록, 코드에는 절대 포함 안 함
- Private 저장소 + Actions는 GitHub Free 플랜에서 완전 무료 (GitHub
  Pages와 달리 Private 상태로도 무료 사용 가능 — 이 프로젝트는 Pages
  안 씀, 백그라운드 실행만 함)

---

## 6. 보안 원칙 (반드시 준수)

**핵심 원칙: 코드와 비밀값을 물리적으로 분리한다.** Git은 커밋 이력이
영구히 남기 때문에, 나중에 지우는 방법을 고민하는 대신 애초에 비밀값이
커밋될 수 없는 구조를 만든다.

1. **`.env` 파일**: API 키/토큰 등 모든 비밀값은 이 파일에만 저장. 코드에는
   `os.getenv("KEY_NAME")` 형태로만 참조, 실제 값은 절대 하드코딩 안 함
2. **`.gitignore`**: `.env`, `kis_devlp.yaml`을 반드시 포함 — Git이 이 파일들을
   추적 대상에서 원천 제외하도록 함
3. **GitHub Secrets**: GitHub Actions 실행 시에는 `.env`가 없으므로, GitHub
   저장소 Settings에 암호화 등록한 값을 환경변수로 주입받음
4. **저장소는 반드시 Private**으로 생성
5. **실제 키/토큰 값은 Claude Code에게 텍스트로 붙여넣지 않는다** — 사용자가
   직접 파일을 열어서 입력. Claude Code는 "자리(placeholder)"만 만들어줌
6. 매 STEP마다 `git add` 전에 `git status`로 무엇이 커밋 대상에 포함되는지
   확인하는 습관화
7. 이 프로젝트는 조회+알림 전용이라 매매 권한 자체가 없음 — 만약 키가
   유출돼도 실제 자금 피해로 이어지지 않음 (Phase 2로 갈 때는 이 전제가
   바뀌므로 별도로 재검토 필요)

---

## 7. 사전 준비 완료 상태

- [x] KIS 실전투자 App Key/Secret 재발급 완료
- [x] Git, uv, Node.js, VS Code, Claude Code 설치 완료
- [x] `open-trading-api` 저장소 클론 + `uv sync` 완료
- [x] `kis_devlp.yaml` 사본을 `~/KIS/config/`에 저장, 실전투자 키 입력 완료
- [x] `.gitignore`에 `kis_devlp.yaml` 추가 완료
- [x] 삼성전자 현재가/일봉 조회 테스트 성공 (인증 정상 작동 확인됨)
- [x] 텔레그램 봇 생성 완료, 봇 토큰 발급받음 (Chat ID는 아직 미확인일 수 있음)

---

## 8. 실행 계획 (STEP 0 ~ STEP 10)

각 STEP은 "작업" → "검증" → 사용자 확인 → 다음 STEP 순서로 진행한다.

### STEP 0. 환경 상태 확인
**작업**: `git status` 실행
**검증**: "nothing to commit, working tree clean" 확인. 다르면 멈추고
변경사항 사용자에게 보고.

### STEP 1. `.env` 파일 자리 만들기
**작업**: 프로젝트 루트에 `.env` 생성 (`TELEGRAM_BOT_TOKEN=`,
`TELEGRAM_CHAT_ID=` 키 이름만, 값은 비움). `.gitignore`에 `.env` 추가.
**검증**: `git status`로 `.env`가 추적 안 되는지 확인.
**멈춤**: 사용자가 직접 `.env`에 실제 토큰/Chat ID 입력할 때까지 대기.

### STEP 2. 해외주식 시세조회 단일 테스트
**작업**: `examples_llm/overseas_stock`에서 NVDA 최근 20일 일봉 조회
테스트.
**검증**: OHLCV 데이터 정상 출력 확인. 실패 시 에러 원인 진단.

### STEP 3. 32개 종목 리스트 조회 확장
**작업**: 섹션 2의 32개 종목/ETF로 순회 조회 확장 (API 호출 제한 고려해
딜레이 포함), 일봉 250봉/주봉 104봉.
**검증**: "N/32 성공" 카운트 확인. 실패 종목 있으면 원인 표시.
**멈춤**: 32개 대부분 성공 확인 후 진행.

### STEP 4. 기본 지표 계산 함수 구현
**작업**: 섹션 3-1의 RSI/MFI/볼린저/%B/Bandwidth/ADX/20주선 계산 함수 구현.
**검증**: NVDA로 계산 실행, 지표값이 상식적 범위인지 확인 (NaN/이상값 없음).

### STEP 5. 파생 신호 구현
**작업**: 섹션 3-2의 다이버전스/스퀴즈/선행반등/거래량소진 함수 구현.
**검증**: NVDA로 개별 실행, 최근 6개월간 각 신호 발생 횟수 확인 (0번이면
버그 의심, 과도하게 많으면 조건 완화 필요).

### STEP 6. 스코어링 함수 구현 (핵심)
**작업**: 섹션 3-3 공식 그대로 `calculate_buy_score()` 구현.
**검증**: NVDA 최근 6개월 스코어 시계열을 가격차트와 겹쳐 시각화, 스코어
급등 시점과 실제 반등 시점이 대체로 겹치는지 육안 확인.
**멈춤**: 그래프 확인 후 사용자 판단으로 진행.

### STEP 7. 32개 종목 전체 파이프라인 (콘솔 출력만)
**작업**: STEP 3 데이터 + STEP 6 스코어링 결합, 종목별 점수/판정/세부신호
표 출력 + JSON 이력 저장.
**검증**: `uv run python main.py` 실행, 32개 종목 점수 전부 출력 확인.

### STEP 8. 텔레그램 알림 연동
**전제**: STEP 1에서 `.env`에 실제 값 입력 완료 상태.
**작업**: 상태 변화 감지(어제 미만→오늘 이상) 로직 + 텔레그램 발송 함수.
**검증**: 실제 텔레그램 메시지 수신 확인, 중복 알림 안 오는지 재실행
테스트.

### STEP 9. Private 저장소 + GitHub Actions 자동화
**작업**:
1. GitHub Private 저장소 생성, 코드 push (`.env`/`kis_devlp.yaml` 제외 확인)
2. GitHub Secrets에 `KIS_APP_KEY`, `KIS_APP_SECRET`, `KIS_ACCT_NO`,
   `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` 등록
3. `.github/workflows/daily.yml` 작성 (매일 한국시간 07:30 cron,
   `workflow_dispatch` 수동 트리거 포함)
**검증**: Actions 탭에서 수동 실행(Run workflow), 로그 정상 확인, 텔레그램
수신 확인.
**멈춤**: 수동 실행 성공 확인 후 자동 스케줄 대기 상태로 전환.

### STEP 10. 관찰 및 튜닝 (별도 세션, 며칠~몇 주 후)
**작업**: 누적된 JSON 이력과 실제 가격을 비교해 신호 발생 후 5/10/20일
수익률 분석, 어떤 하위신호가 예측력 높았는지 평가.
**검증**: 임의 매수 대비 유의미한 우위 확인. 필요시 가중치/임계값 조정 후
STEP 7부터 재검증.

---

## 10. 모니터링 대시보드 설계

**목적**: 32개 종목의 스코어를 한눈에 훑으면서, "지금 매수 타이밍으로
볼만한 종목"을 빠르게 걸러내고, 클릭해서 근거(왜 그 점수인지)까지
확인할 수 있게 한다. 단순 점수 나열이 아니라 **정보 위계(한눈에 →
근거 확인)** 구조로 설계.

**보안 구조 (섹션 6 원칙과 동일)**
```
[private repo: GitHub Actions] → 스코어 계산 → 결과(점수만, API키 없음)를
[해당 repo의 docs/ 폴더 또는 별도 public repo]: scores.json 으로 push
    ↓
[GitHub Pages 대시보드]: scores.json 읽어 렌더링 → 모바일 브라우저로 확인
```
- API 키, 계좌정보, 주문 관련 데이터는 절대 scores.json에 포함하지 않음
- scores.json에는 종목코드/점수/세부신호/현재가/타임스탬프만 포함

### 10-1. 레이아웃 구조 (위→아래 정보 위계)

**① 상단: 오늘의 신규 신호 배너 (가장 중요, 최상단 고정)**
- "어제 대비 새로 70점 이상 진입한 종목"만 카드로 크게 표시
- 없으면 "오늘 신규 신호 없음" 문구로 배너 접힘
- 이 섹션이 곧 "지금 봐야 할 것"의 답이므로 스크롤 없이 바로 보여야 함

**② 전체 종목 그리드 (스코어 내림차순 정렬)**
각 종목 카드에 포함할 정보:
- 티커 + 종목명
- **최종점수를 큰 숫자 + 색상**으로 (70+ 빨강 계열, 50~70 주황, 50미만 회색 — 점수는 "신호강도"이므로 등락률 색상과 별도 체계 사용)
- 현재가 + 당일 등락률 (기존 관례: 상승 빨강/하락 파랑 유지)
- **세부신호 배지** (다이버전스/스퀴즈/MFI선행/거래량소진 중 어떤 게 켜졌는지 아이콘 4개로 표시, 켜진 것만 강조색)
- **미니 스파크라인**: 최근 30일 점수 추이를 작은 선그래프로 (점수가 오르는 중인지 꺾이는 중인지 한눈에)

**③ 종목 카드 탭하면 상세 뷰로 확장**
- 가격 차트(일봉) + 볼린저밴드 오버레이
- RSI/MFI 서브차트 (30/70, 20/80 기준선 표시)
- 점수 구성 breakdown: 6개 항목 각각 몇 점씩 기여했는지 막대 (주봉추세/%B/다이버전스/MFI선행/스퀴즈/거래량 — 왜 이 점수가 나왔는지 즉시 이해 가능)
- ADX 국면 표시(추세장/횡보장) — 페널티 적용 여부 명시

**④ 필터/정렬 (그리드 상단 툴바)**
- 정렬: 점수순 / 등락률순 / 알파벳순
- 필터: 전체 / 강한후보(70+) / 관찰대상(50~70) / 카테고리별(빅테크/산업대장/ETF)

### 10-2. 모바일 레이아웃 (Galaxy Z Fold7 기준, 기존 대시보드 관례 재사용)
- 폴드 상태(~390px): 신규신호 배너 → 종목 그리드 1열 카드 세로나열
- 언폴드 상태(~904px): 신규신호 배너 → 종목 그리드 2~3열
- 기존 `fold-hide` CSS 클래스 패턴 재사용 가능

### 10-3. 색상 원칙 (혼동 방지 필수)
- **점수(신호강도) 색상**: 빨강(강)/주황(중)/회색(약) — "매수신호가 세다"의 의미
- **등락률 색상**: 기존 한국 관례 그대로 상승=빨강/하락=파랑 — "가격이 올랐다"의 의미
- 이 둘이 시각적으로 겹치지 않게(같은 카드 안에 둘 다 있으므로) 명도/채도 차이나 배지 형태로 명확히 구분

### 10-4. 구현 방식
- 단일 `index.html` (기존 패턴 그대로), Canvas 또는 경량 차트 라이브러리로 렌더링
- `scores.json`을 fetch해서 렌더링 (별도 백엔드 서버 불필요, GitHub Pages 정적 호스팅으로 충분)
- localStorage로 "마지막 확인한 신호" 기억해서, 새 신호만 강조 표시 유지 가능

---

## 11. 실행 계획 추가 STEP

### STEP 11. 모니터링 대시보드 구축 (별도 세션 권장)
**전제**: STEP 9(GitHub Actions 자동화)까지 정상 동작 확인된 상태

**작업**
1. GitHub Actions 워크플로우 마지막 단계에 "scores.json 생성 + 별도
   public 경로로 push" 단계 추가 (섹션 10 구조대로, API키/계좌정보
   제외 확인)
2. `docs/index.html` 대시보드 파일 작성 — 섹션 10-1~10-4 스펙 그대로
3. 저장소 Settings → Pages에서 `docs/` 폴더를 Pages 소스로 설정
   (이 폴더가 있는 저장소는 이 부분만 public 저장소로 분리하거나,
   scores.json만 담는 별도 public repo 사용 — API 키 있는 private repo와
   절대 혼동하지 않도록 확인)

**검증**
- 배포된 Pages URL에서 대시보드 정상 렌더링 확인
- 실제 오늘자 스코어 데이터가 반영되는지 확인
- 모바일 브라우저(폴드/언폴드 상태 둘 다)에서 레이아웃 확인
- 신규 70점 종목이 있을 때 상단 배너가 정상 강조되는지 확인 (테스트로
  임계값 잠시 낮춰서 확인 가능)

---

## 12. 진행 현황 요약 (2026-07-25 기준)

- STEP 0~12(전제조건 1~5) 전부 완료. 매일 한국시간 07:30 자동 실행 중
  (조회→스코어링→텔레그램 알림→대시보드 갱신).
- 저장소: `https://github.com/withgest-lab/kis-buy-signal` (Public)
- 대시보드: `https://withgest-lab.github.io/kis-buy-signal/` (GitHub Pages
  소스 = `data` 브랜치, 매일 이력 없이 덮어쓰는 방식으로 발행)
- 타겟 종목: 32개 → 92개 → **543개**(섹터ETF 25 + S&P500∪나스닥100 개별종목
  518)로 확장 완료. 개별종목 리스트는 `signal_bot/universe_source.py`가
  월 1회 자동 갱신+캐시. 조회는 스레드풀 동시 호출(최대 5개, 초당 8건 상한
  레이트리미터)로 처리, 543종목 기준 약 7~8분 소요.

## 13. STEP 12. S&P500 + 나스닥100 전체 확장

**배경**: 사용자가 "미장 전체 시장, 전체 우량 종목을 일별 센싱해서 매수 적기를
파악하고 자동 알림을 받는 것"이 최종 목적이라고 밝힘. 92개 → S&P500(500개) +
나스닥100(100개) 중복제거 후 약 550~600개로 확장 희망. 2026-07-25 논의 결과
기술적으로 가능하다고 판단(동시조회 방식이면 약 7~10분 소요 예상, 사용자는
하루 1회 실행이라 속도 저하 자체는 문제없다고 확인함).

**전제조건 1~4 진행 상태 (2026-07-25 세션에서 처리)**:

1. ✅ **종목 리스트 소스+캐시**: `signal_bot/universe_source.py` 신규 추가.
   S&P500은 위키피디아(1차)→slickcharts.com(2차), 나스닥100은 slickcharts.com
   (1차)→stockanalysis.com(2차) 순으로 시도. **실제로 위키피디아 "Nasdaq-100"
   문서엔 더 이상 구성종목 표가 없는 걸 이번에 확인함** — 페이지 구조가
   바뀌면 깨진다던 우려가 실제로 이미 벌어져 있었던 사례라, 소스를 지수당
   1개가 아니라 2개씩 준비해두는 걸로 설계를 보강함. 결과는
   `signal_bot/data/universe_cache.json`에 캐시(월 1회 이상 안 됐으면 재조회
   생략), 소스가 전부 실패해도 기존 캐시로 자동 fallback. 실제 실행 결과
   S&P500 503개 + 나스닥100 103개 → 중복 88개 제거 후 **518개** 확보 확인.
   **아직 `config.TICKERS`에는 연결 안 함** (5번 항목에서 사용자 확인 후 연결).

2. ✅ **`history.json` 롤링 윈도우**: `signal_bot/config.py`에
   `HISTORY_MAX_DAYS = 180` 추가, `pipeline.py`에 `trim_history()` 적용.
   최근 180거래일 초과분은 저장 시 자동 삭제. 단위테스트로 200일→180일 정리
   확인.

3. ✅ **대시보드 데이터 전용 브랜치**: `myrepo`에 `data` 브랜치를 orphan
   commit으로 생성. `.github/workflows/daily.yml`이 매일 `docs/`
   (index.html+scores.json+detail/) 스냅샷을 이력 없이 덮어쓰는
   (commit-tree + force push) 방식으로 발행. **사용자가 GitHub Settings >
   Pages에서 소스를 `data` 브랜치로 전환하고, Deployments 목록에서 최신 배포가
   `data` 기준인 걸 직접 확인함(2026-07-25).** 이후 `main`의 과도기용
   `docs/scores.json`/`docs/detail/*.json` 발행은 제거하고 `.gitignore`에도
   추가 — `main`은 이제 코드 커밋만 쌓임.

4. ✅ **견고성 강화**: `fetch_universe.py`에 1차 조회 실패 종목만 모아
   재시도하는 2차 패스 추가, 최종 성공률이 90% 미만이면 텔레그램으로
   "데이터 품질 경고" 별도 발송. 실패 시뮬레이션으로 정상 동작 확인
   (3/92 실패=경고 안 옴, 15/92 실패=경고 발송됨).

5. ✅ **실제 전환 완료 (2026-07-25)**: `config.TICKERS`를 92개 curated
   리스트에서 **섹터ETF 25개(GICS 11개 대표 섹터 SPDR 전부 커버하도록 XLK
   추가) + 개별종목 518개(S&P500∪나스닥100) = 543개**로 교체. 카테고리를
   "개별종목"/"섹터ETF" 2종으로 단순화, 대시보드 필터 칩도 맞춰 수정.
   **통합 테스트 중 발견해서 함께 고친 버그**: KIS API는 클래스주(BRK-B,
   BF-B)를 하이픈이 아니라 슬래시(BRK/B) 표기로만 인식함 — `kis_client.py`에서
   API 호출 직전에만 변환하도록 수정, 파일명/JSON 키 등 내부 표현은 하이픈
   유지.
   **실측 결과**: 조회(fetch_universe) 7분 19초·543/543 성공(계획서 예상
   7~10분과 일치) → 스코어링(main.py) 45초 → 대시보드 발행(export_dashboard)
   43초. 텔레그램은 신규 신호 없어 미발송(정상). 543/543 종목 점수 산출
   완료(강한매수후보 2개, 관찰대상 54개).

**추가로 발견된, 아직 결정 안 한 사항**: `history.json`이 543종목 기준 하루치
약 376KB로 커짐 — 180일 롤링 윈도우로 파일 자체 크기는 최대 약 67MB로
유한하지만(2번 항목), 이 파일은 `data` 브랜치처럼 orphan으로 덮어쓸 수 없고
`main`에 매일 정상 커밋되어야 함(어제-오늘 비교를 위해 진짜 이력이 필요해서).
그래서 `main`의 git 이력 자체는 매일 조금씩 계속 늘어남(연간 대략
수십MB 안팎으로 추정, 정확한 수치는 GitHub의 델타 압축 방식에 따라 다름).
당장 문제될 수준은 아니라고 판단했지만, 원래 4가지 전제조건엔 없던 사항이라
사용자에게 별도로 보고하고 필요 여부를 확인하기로 함.

**다음 세션 시작 방법**: STEP 12 5번까지 전부 완료. 이제 이 프로젝트는
"STEP 10. 관찰 및 튜닝" 단계로 자연스럽게 넘어감(누적 데이터 쌓이면
가중치/임계값 재검증).

---

## 14. STEP 10 완료 기록 (2026-07-26) — 백테스트 기반 유효성 검증 + 가중치 개정

history.json이 쌓이길 기다리지 않고, `signal_bot/backtest_fetch.py`로 543종목
10~13년치 일봉/주봉을 따로 받아서(운영 250일치와 분리, `.gitignore` 처리)
백테스트를 진행. 사용자가 끝까지 검증을 요청해서 하룻밤 동안 반복 검증함.

**1차 결과 (5~20일 단기)**: 국면별(상승장/횡보장/하락장)로 나눠보니 하락장에서
신호가 기준선보다 나빴음(-2.70%p, 20일 기준). 항목별 기여도(ablation) 분석
결과 weekly_trend·percent_b가 하락장에서 과다출현(각 0.54배·1.61배가 아니라
1.61배/기준 대비, RSI다이버전스는 2.21배)하는 걸 확인, Bollinger 본인의
경고("밴드 터치는 확인 필요")·Cardwell의 다이버전스 이론과 실제로 일치함을
검증. 가중치를 15/15/25/20/15/10(주봉추세/%B/다이버전스/MFI선행/스퀴즈/거래량)
으로 재분배 + SPY 200일선 기준 시장 전체 하락장 필터 추가를 시도했으나,
**거래비용(왕복 0.6%) 반영 시 5~20일 우위 자체가 사라짐**을 확인.

**2차 결과 (사용자의 실제 경험 기반 재검증, 3~6개월=63~126거래일)**: 사용자가
"애플/테슬라/구글/마소 등 3개월 이상 보유 시 성공률이 좋았다"고 언급 → 보유기간을
넓혀 재검증. **126일(약 6개월) 기준으로는 거래비용 반영 후에도 뚜렷한 우위
확인**(전체 +1.00~1.28%p, 고변동성/성장주 그룹 +3.38~3.72%p). 13년 중 2015년
한 해만 마이너스, 나머지 12년 플러스. 166개 종목 중 165개가 기여(소수 종목
쏠림 아님). 학계에서 가장 널리 재현된 발견 중 하나인 모멘텀 효과
(Jegadeesh-Titman 1993, "거래비용 반영해도 살아남음")와 부합.

**최종 반영 (사용자 확인 후 커밋 완료)**:
1. `scoring.py` WEIGHTS를 15/15/25/20/15/10으로 개정 (확인성 신호 비중 확대)
2. `market_regime()`/`apply_market_filter()` 추가 — SPY가 200일선 대비 -3%
   이하(하락장)면 신규 강한매수후보 진입 자체를 차단 (`pipeline.py`에서 매 실행시
   판정)
3. 텔레그램 알림(`alerts.py`)에 오늘의 시장국면 + "5~20일 단기로는 우위 없고
   3~6개월 이상 보유 시 거래비용 반영해도 우위 확인됨"이라는 정직한 안내문 추가
4. 대시보드(`docs/index.html`, `export_dashboard.py`)에 **주봉 차트 신규 추가**
   (일봉은 유지), RSI/MFI를 별도 차트 2개 대신 **한 차트에 색깔 다르게(주황/초록)
   겹쳐서 표시**, 상단에 오늘의 시장국면 표시, 도움말에 3~6개월 보유 근거 추가.
   Playwright로 실제 렌더링 확인 완료.
5. 재사용 가능한 백테스트 도구 4종(`backtest_fetch.py`, `backtest_components.py`,
   `backtest_experiments.py`, `backtest_report.py`) 커밋 — 다음에 데이터가 더
   쌓이면 같은 도구로 재검증 가능.

**남은 caveat**: 이 결론은 13년치 과거 데이터 기반이며, 실제 미래 성과를
보장하지 않음. 앞으로 몇 달간 실제 알림 이력이 쌓이면 진짜 아웃오브샘플
검증(계획서 원래 STEP 10 취지)으로 다시 확인하는 게 이상적.

### 14-1. 종합 의견 — 이 도구가 매매에 실제로 주는 도움 (2026-07-26)

**"그럴듯해 보이는데 실제로 도움이 되는가"에 대한 최종 답**: 5~20일 단기
반등 기준으로는 거래비용 반영 시 우위가 사라졌지만, **3~6개월 이상 보유
기준으로는 13년 중 12년 플러스, 거래비용 반영 후에도 통계적으로 유의미한
우위**를 확인함. 재무학에서 가장 널리 재현된 발견 중 하나인 모멘텀 효과
(Jegadeesh-Titman 1993 — 3~12개월 잘 나가던 종목이 그 이후도 계속 잘 나가는
경향, "거래비용을 감안해도 살아남는다"고 원 논문에 명시)와 정확히 부합.

**구체적으로 어떤 도움을 받을 수 있나**
1. **543종목을 사람이 매일 다 못 보는 걸 대신 걸러줌** — 가장 실용적인 가치.
2. **"떨어지는 칼날 잡기" 방지** — 시장 전체가 하락 국면일 땐 신규 강한매수
   신호 자체가 안 뜨도록 막아둠. 자체 데이터로는 확정 못 했지만(표본 부족),
   Faber(2007)·Siegel(1886~2006, 120년치)의 200일선 시장타이밍 연구와
   Bollinger 본인의 경고("밴드 터치는 확인이 필요하다")가 강하게 뒷받침.
3. **다이버전스·MFI선행·스퀴즈확장 조합**은 Cardwell(RSI 다이버전스 권위자)·
   Granville(거래량 선행 이론)의 방법론과 실제로 대조 검증했고, 데이터에서도
   일관되게 기여도가 확인된 항목들.

**정직한 한계**
- 20일 이내 짧은 반등을 노리는 용도로는 근거 없음 — "3~6개월 이상 들고 갈
  후보 압축 도구"로 쓰는 게 맞음.
- 13년치 과거 데이터 기반이라 미래를 보장하지 않음.
- 최종 매수/매도 판단과 보유기간은 항상 사용자가 직접 결정하는 게 이
  시스템의 기본 전제(Phase 1: 조회+알림 전용, 자동매매 없음).

### 14-2. 최종 보안 점검 (2026-07-26)

| 항목 | 결과 |
|---|---|
| `.env`/`kis_devlp.yaml` 커밋 여부 | 안전 — gitignore 처리, 커밋 이력 전체 확인해도 실제 키 노출 없음(과거 한 번 추적된 적 있었으나 내용은 플레이스홀더였고 이미 삭제됨) |
| GitHub Actions 시크릿 처리 | 안전 — 시크릿은 저장소 폴더 밖(`~/KIS/config/`)에만 파일로 쓰이고, 로그 출력 코드 없음 |
| 워크플로우 권한 범위 | 안전 — `contents: write`만 부여, 외부 PR로 시크릿 탈취 가능한 `pull_request` 트리거 없음 |
| 하드코딩된 키/토큰 | 안전 — 소스 전체 검색해도 없음 |
| **대시보드 XSS** | **발견 → 수정 완료** — 종목명이 위키피디아 등 제3자 편집 가능 소스에서 오는데 이스케이프 없이 화면에 삽입되고 있었음. `escapeHtml()` 추가해서 소스 훼손 시에도 스크립트 실행 안 되도록 방어(`docs/index.html`) |

### 14-3. 알림 시각/횟수 검토 (2026-07-26) — 07:30 · 1일 1회 유지 결론

사용자가 미국장 운영시간 대비 알림 시각(KST 07:30)과 빈도(1일 1회)가
적절한지 물어서 검토. **결론: 지금 그대로가 적절, 변경 불필요.**

- **마감 후 버퍼 충분**: 미국장 마감(오후 4시 동부시간)은 서머타임 여부에
  따라 UTC 20:00(서머타임 적용, 3~11월) 또는 UTC 21:00(서머타임 해제)인데,
  알림 시각(UTC 22:30=KST 07:30)까지 각각 2.5시간·1.5시간 버퍼가 있어
  KIS가 그날 종가를 확정하기에 충분함. 실제로 데이터 누락 문제 없었음.
- **하루 1회로 충분한 근본 이유**: 이 시스템은 일봉 기반 — RSI/MFI/볼린저/
  ADX 전부 하루 한 번만 갱신됨. 하루에 여러 번 돌려도 같은 날짜 데이터를
  또 계산할 뿐 새 정보가 없어 API 호출만 낭비.
- **STEP 10 검증으로 "정확한 시각"의 중요도가 더 낮아짐**: 이 신호의 실제
  근거가 3~6개월 보유 기준이라, 하루 중 몇 시간 일찍/늦게 보든 몇 달짜리
  판단엔 차이가 없음. 원래 "사용자 기상 시간대"에 맞춘 07:30을 바꿀 실익 없음.
- **바꿔야 하는 경우**: 나중에 "장중 실시간 급락 감지" 같은 완전히 다른
  성격(분봉/실시간호가 필요)의 기능을 추가한다면 그건 별도 설계가 필요함
  — 지금 목적(3~6개월 보유 후보 스크리닝)엔 해당 없음.

### 14-4. GOOGL 실사례 계기 3가지 추가 검증 (2026-07-26)

사용자가 실제 종목(GOOGL) 점수가 하루 만에 53.8→15.0으로 급락하는 걸
발견해서 제기한 3가지를 데이터로 검증.

1. **신호 지속성(1일만 감지되는 문제)**: 다이버전스/스퀴즈확장/MFI선행이
   "오늘 하루"만 조건을 만족하면 켜지는 구조라, GOOGL처럼 급락 당일
   감지됐다가 다음날 소폭 반등만으로 꺼지는 현상 확인. **지속성을
   2/3/5일로 늘려 백테스트해본 결과, 오히려 우위가 줄어듦**(OOS 126일
   기준 1일=+2.45%p, 2일=+1.49%p, 3일=+1.61%p, 5일=+1.16%p) — "감지된
   바로 그날"이 가장 신선한 신호이고 그 이후는 상대적으로 덜 특별한
   진입점이기 때문으로 추정. **결론: 점수/알림 로직은 1일 그대로 유지**,
   대신 대시보드에 "최근 3일 내 감지 이력" 정보용 배지(점선 테두리)만
   추가해서 점수에 영향 없이 참고할 수 있게 함.

2. **알림 문턱(70→65)**: 사용자가 "얼리 센싱"이 목적이니 문턱을 낮추는
   게 낫지 않겠냐고 제안 → 60/65/70 백테스트 비교 결과 65점이 70점과
   거의 동일한 우위(OOS 126일 +2.36%p vs +2.45%p)를 유지하면서 신호는
   1.8배 더 잡음(60점은 +1.21%p로 우위 절반 희석돼서 기각). **65점으로
   확정**, verdict 기준(강한매수후보/관찰대상)도 65/50으로 통일.

3. **국면별(상승/횡보/하락) 차등 가중치**: 사용자가 "횡보장·하락장은
   눌림목이 잘 맞고 상승장은 애매하다"며 국면별로 다른 가중치를 쓰는
   걸 검토해달라고 요청. 각 컴포넌트의 국면별 126일 forward return 상관계수를
   계산해서 가중치 후보를 만들고 실제 백테스트로 검증한 결과, **국면별
   가중치가 단일 가중치보다 뚜렷이 나쁨**(OOS 126일 전체 기준 +2.36%p →
   +0.13%p로 악화). 원인: 상관계수 자체가 약하고(전부 0.15 이하) 일부는
   기존 ablation 결과와 모순(예: 스퀴즈확장이 하락장에서 상관계수는
   음수인데 실제로는 하락장 보호 효과가 가장 컸음) — 노이즈에 가중치를
   맞춘 격. **결론: 국면별 가중치 도입 안 함, 단일 가중치(15/15/25/20/15/10)
   유지**. 국면 차이는 기존처럼 "하락장 진입 차단" 필터만으로 반영.

   참고로 "횡보장+과매도(%B<=0.1 & RSI/MFI<=35)" 원시 조합 자체의 유효성은
   확인됨(63일 +1.16%p, 126일 +1.05%p, 20일은 오히려 -0.33%p로 단기엔
   무의미) — 다만 이걸 "가중치를 국면별로 나누는" 방식이 아니라 지금처럼
   "충분히 긴 보유기간(3~6개월)을 전제로 한 단일 모델"로 자연스럽게
   흡수되고 있다고 판단.

---

## 9. 이 세션에서 지켜야 할 대화 원칙

- 매 STEP 완료 시 다음 STEP으로 자동으로 넘어가지 말고, 결과를 보여준 뒤
  사용자의 명시적 확인을 기다릴 것
- 사용자는 Git/프로그래밍 초보이므로, 명령어 실행 결과나 에러가 발생하면
  전문용어를 풀어서 설명할 것
- API 키, 토큰 등 민감정보는 채팅에 직접 값으로 출력하거나 요구하지 말 것
- Phase 1 범위(조회+알림)를 벗어나는 주문/매매 관련 코드는 사용자가
  명시적으로 요청하기 전까지 절대 구현하지 말 것
